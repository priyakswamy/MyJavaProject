import java.util.Arrays;
import java.util.Scanner;

public class Comparing_numbers {
	/*void isSecondHighest(double param, double num1, double num2, double num3, double num4){
		
		//5 6 2 3 4
		int count = 0;
		// 4 < 6
		if( param > num1 ){
			count = count + 1;
		}
		// 4 < 2
		if( param > num2 ){
			count++;
		}
		
		// 4 < 3
		if( param > num3 ){
			count++;
		}
		
		// 4 < 4
		if( param > num4 ){
			count++;
		}

		if(count == 1){
			System.out.println("Second highest value="  +param);
		} 

	}*/
	public static void main(String[] args) {
		//Comparing_numbers  Cn= new Comparing_numbers();
		/*double a = 4;
		double b = 6;
		double c = 2;
		double d = 3;
		double e = 5;*/

		//int[] arr = {4,6,2,3,5};
	
		
	}
		//

		/*Cn.isSecondHighest(a, b,c,d,e);
		Cn.isSecondHighest(b, a,c,d,e);
		Cn.isSecondHighest(c, b,a,d,e);
		Cn.isSecondHighest(d, b,c,a,e);
		Cn.isSecondHighest(e, b,c,d,a);
*/
		
		

	/*	if (high < price2) {
			high = price2;
		} else if (low > price2) {
			low = price2;
		} else if (high < price3) {
			high = price3;
		} else if (low > price3) {
			low = price3;
		} else if (high < price4) {
			high = price4;
		} else if (low > price4) {
			low = price4;
		} else if (high < price5) {
			high = price5;

		}

		else {
			low = price5;
		}
		System.out.println(high);
		System.out.println(low);*/
	}

	

